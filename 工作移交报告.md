# 课表自动获取工具工作移交报告

## 1. 项目基本信息

- **项目名称**：安徽建筑大学研究生课表自动获取工具
- **项目目录**：`D:\ruanjian\Kivio\program\课表`
- **代码仓库**：https://github.com/dabai214109/ahjzu-schedule
- **仓库可见性**：Private
- **当前分支**：`main`
- **当前提交**：`970de8d150765c16e7eed8772f632b6767032341`
- **报告日期**：2026 年 9 月 15 日

## 2. 项目目标

通过安徽建筑大学统一身份认证和 WebVPN，获取研究生系统中的个人课表，并通过适配手机浏览器的网页界面展示课程、教师、教室、周次和节次信息。

当前实现形态为 **Flask 后端 + 手机网页前端**。后续可以在此基础上封装为 PWA、Android 应用或桌面小组件。

## 3. 已完成内容

### 3.1 后端

- 创建 Flask 应用入口 `app.py`。
- 配置安徽建筑大学 SSO、信息门户和研究生系统地址。
- 实现基于 `requests.Session` 的登录会话管理。
- 实现 WebVPN `S(...)` 会话标识提取。
- 根据浏览器抓包信息，确认并接入课表接口：
  - 请求地址：`/gmis5/(S(...))/student/pygl/py_kbcx_ew`
  - 请求方式：`POST`
  - 请求参数：`kblx=xs&termcode=202701`
  - 请求头：`X-Requested-With: XMLHttpRequest`
- 实现课表 JSON 数据解析，将 `z1` 到 `z7` 映射为周一到周日。
- 实现课程名称、周次、教师、教室、星期和节次的提取。
- 提供以下接口：
  - `GET /`：返回手机端页面
  - `POST /api/login`：执行登录
  - `GET /api/schedule`：返回解析后的课表
  - `GET /api/schedule/raw`：返回原始课表接口结果，便于调试

### 3.2 前端

- 创建 `templates/index.html`。
- 实现学号和密码登录页面。
- 实现移动端响应式课表网格。
- 显示上午、下午和晚上节次。
- 支持点击课程查看教师、教室、星期、节次和周次。
- 支持退出登录。
- 添加课程颜色区分和当天高亮显示。

### 3.3 工程文件

- `requirements.txt`：Flask、Requests、BeautifulSoup4 依赖。
- `README.md`：项目说明和运行方法。
- `.gitignore`：忽略虚拟环境、缓存、IDE 配置和本地敏感文件。

## 4. 关键接口与数据格式

### 4.1 课表接口

浏览器抓包得到的请求格式如下：

```text
POST https://219-231-15-133.webvpn.ahjzu.edu.cn/gmis5/(S(...))/student/pygl/py_kbcx_ew
Content-Type: application/x-www-form-urlencoded;charset=UTF-8
X-Requested-With: XMLHttpRequest

kblx=xs&termcode=202701
```

其中：

- `S(...)` 是每次会话可能变化的 WebVPN 会话标识。
- `kblx=xs` 表示学生课表。
- `termcode=202701` 表示当前配置的学期代码，需要随学期调整。

### 4.2 课表返回结构

- `rows`：节次数组。
- `jcid`：节次 ID。
- `sjbz`：时间段，例如上午、下午、晚上。
- `mc`：节次名称，例如上午 1、下午 6。
- `z1` 到 `z7`：周一到周日的课程内容。
- `week`：接口返回的星期信息。

课程单元的典型文本格式为：

```text
课程名称班级[1-8周] 教师[教室]
```

## 5. 本地运行方法

在项目目录打开 PowerShell：

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python app.py
```

浏览器访问：

```text
http://127.0.0.1:5000
```

如需手机访问：

1. 电脑和手机连接同一局域网。
2. 在电脑执行 `ipconfig`，找到局域网 IPv4 地址。
3. 手机访问 `http://电脑IPv4地址:5000`。
4. 如无法访问，检查 Windows 防火墙是否允许 Python 使用专用网络。

## 6. 当前未完成和风险项

以下内容在移交时必须明确：

1. **尚未完成真实账号登录验证**。当前代码没有使用用户账号密码进行联调，因此 SSO 表单字段、验证码处理和 WebVPN 跳转是否完全兼容，仍需实际测试。
2. **依赖尚未完成安装验证**。此前安装命令被运行环境拦截，需在本机虚拟环境中执行安装。
3. **学期代码是固定值**。`app.py` 中的 `DEFAULT_TERM = "202701"` 需要根据教务系统实际学期代码修改。
4. **登录页可能存在验证码要求**。截图显示登录页面有验证码。当前后端只提交用户名、密码和隐藏字段，没有实现验证码识别或人工输入流程；如果 SSO 强制验证码，需要增加验证码展示和提交步骤。
5. **会话存储方式仅适合原型**。当前把上游 cookies 放入 Flask 默认客户端 session，生产使用时可能受到 Cookie 大小、泄露和过期问题影响，应改为服务端 session 存储，并使用环境变量配置密钥。
6. **Flask 密钥不能继续硬编码**。`app.py` 中的 `app.secret_key` 仅为开发占位值，正式使用前必须更换，并从环境变量读取。
7. **前端周次切换仍需修正**。当前周次按钮已经生成，但课表渲染逻辑没有根据所选周次过滤课程，后续需要按课程周次判断当前周是否上课。
8. **节次时间是近似配置**。前端的 `SLOT_TIMES` 目前是手动配置，应根据学校实际作息时间确认或改为从系统读取。
9. **没有自动刷新和缓存机制**。目前每次访问课表接口都会重新请求教务系统，后续可增加手动刷新、定时刷新和本地缓存。
10. **暂未打包成真正的手机 App**。当前版本是手机网页应用，可先通过浏览器使用，后续再选择 PWA、Capacitor、Flutter 或 React Native 封装。

## 7. 建议接手顺序

1. 在本机创建并激活 Python 虚拟环境。
2. 安装 `requirements.txt` 中的依赖。
3. 运行 Flask 服务，确认首页正常打开。
4. 使用测试账号验证 SSO 登录，不要在代码、GitHub 或报告中记录密码。
5. 查看终端日志，确认 WebVPN 会话标识被正确提取。
6. 调用 `/api/schedule/raw`，确认返回对象中包含 `rows`。
7. 调用 `/api/schedule`，核对课程数量、星期、教师和教室。
8. 修正验证码、学期代码和周次过滤问题。
9. 更换 Flask 密钥并改用环境变量。
10. 完成手机端真机测试后，再决定是否封装为 App。

## 8. 安全要求

- 不要把学号、密码、验证码、Cookie、WebVPN `S(...)` 标识提交到 GitHub。
- 不要把浏览器复制的完整请求头和 Cookie 上传到公开位置。
- 不要在前端保存教务系统密码。
- 正式部署时关闭 Flask `debug` 模式。
- 正式部署时使用随机生成的 `SECRET_KEY`，并限制服务访问范围。
- 如将服务部署到公网，必须配置 HTTPS、登录超时、服务端会话存储和访问控制。

## 9. GitHub 交接信息

代码已上传到：

https://github.com/dabai214109/ahjzu-schedule

初始提交包含：

```text
app.py
requirements.txt
README.md
.gitignore
templates/index.html
```

当前仓库为私有仓库。后续协作时，应通过 GitHub 分支和 Pull Request 管理修改，不要直接把账号信息写入源码。

## 10. 交接结论

项目已经完成原型代码、课表接口接入和手机网页界面搭建，并已推送到 GitHub。当前状态属于 **可继续联调的原型版本**，尚未完成真实登录、验证码兼容性、周次过滤、生产安全配置和手机 App 打包。下一位接手者应优先完成真实账号联调和接口返回验证，再进行功能完善与发布。
